import { trainers } from "@/lib/data";
import Link from "next/link";
export default async function TrainerProfile({ params }: { params: Promise<{ id: string }> }){
 const {id}=await params; const trainer=trainers.find(t=>t.id===Number(id))||trainers[0];
 return <main className="container"><Link className="view-link" href="/trainers">← Back to trainers</Link><section className="profile-hero"><img className="profile-image" src={trainer.image} alt={trainer.name}/><div className="card card-pad"><div className="space"><span className="rating">★ {trainer.rating} ({trainer.reviews})</span>{trainer.verified&&<span className="verified">✓ Verified Trainer</span>}</div><h1 style={{fontSize:56, margin:"12px 0"}}>{trainer.name}</h1><p className="muted">📍 {trainer.city}</p><p><strong>{trainer.speciality}</strong></p><p>{trainer.bio}</p><div className="grid grid-3"><div className="glass-stat"><strong>{trainer.experience}</strong><span>Experience</span></div><div className="glass-stat"><strong>120+</strong><span>Clients</span></div><div className="glass-stat"><strong>{trainer.rating}</strong><span>Rating</span></div></div><div className="info-list"><div className="info-line"><span>1 Session</span><strong>{trainer.price}</strong></div><div className="info-line"><span>4 Sessions</span><strong>LYD 220</strong></div><div className="info-line"><span>8 Sessions</span><strong>LYD 400</strong></div></div><div className="actions"><button className="btn btn-primary">Book Session</button><button className="btn btn-soft">WhatsApp</button></div></div></section></main>
}
