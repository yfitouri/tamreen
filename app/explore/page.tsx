import { categories, gyms, trainers, foodBusinesses, sports } from "@/lib/data";
import { CategoryCard, ListingCard, SectionHead } from "@/components/Cards";

export default function ExplorePage() {
  return <main className="container"><section className="page-hero"><span className="kicker">Explore Libya</span><h1>Where do you want to go?</h1><div className="filters"><input className="input" placeholder="Search gyms, trainers, sports, events..." /></div></section><section className="section"><SectionHead title="Categories" sub="Everything active in one app"/><div className="grid grid-4">{categories.map(x => <CategoryCard key={x.title} item={x}/>)}</div></section><section className="section"><SectionHead title="Sports" sub="Find people, places and events by sport" href="/sports"/><div className="grid grid-3">{sports.map(s => <div className="card card-pad" key={s.name}><span className="category-icon">{s.icon}</span><h3 className="card-title">{s.name}</h3><p className="muted">{s.desc}</p></div>)}</div></section><section className="section"><SectionHead title="Top Rated Near You"/><div className="grid grid-3">{gyms.slice(0,2).map(x=><ListingCard key={x.id} item={x} type="gym"/>)}{trainers.slice(0,1).map(x=><ListingCard key={x.id} item={x} type="trainer"/>)}{foodBusinesses.slice(0,1).map(x=><ListingCard key={x.id} item={x} type="food"/> )}</div></section></main>
}
