"use client";
import { useState } from "react";
export default function Calories(){
 const [age,setAge]=useState(25),[height,setHeight]=useState(175),[weight,setWeight]=useState(80),[gender,setGender]=useState("male"),[activity,setActivity]=useState(1.55);
 const bmr=10*weight+6.25*height-5*age+(gender==="male"?5:-161); const maintenance=Math.round(bmr*Number(activity));
 return <main className="container"><section className="page-hero"><span className="kicker">Calories</span><h1>Simple calorie calculator.</h1><p className="muted">Quick targets for maintenance, cutting, bulking, protein and water.</p></section><div className="profile-hero"><div className="card card-pad"><div className="form-stack"><label>Age<input className="input" type="number" value={age} onChange={e=>setAge(+e.target.value)}/></label><label>Height cm<input className="input" type="number" value={height} onChange={e=>setHeight(+e.target.value)}/></label><label>Weight kg<input className="input" type="number" value={weight} onChange={e=>setWeight(+e.target.value)}/></label><label>Gender<select className="input" value={gender} onChange={e=>setGender(e.target.value)}><option value="male">Male</option><option value="female">Female</option></select></label><label>Activity<select className="input" value={activity} onChange={e=>setActivity(+e.target.value)}><option value={1.2}>Sedentary</option><option value={1.375}>Light</option><option value={1.55}>Moderate</option><option value={1.725}>Active</option><option value={1.9}>Very active</option></select></label></div></div><div className="card card-pad"><h2>Your targets</h2><div className="info-list"><div className="info-line"><span>Maintenance</span><strong>{maintenance} kcal</strong></div><div className="info-line"><span>Cutting</span><strong>{maintenance-400} kcal</strong></div><div className="info-line"><span>Bulking</span><strong>{maintenance+300} kcal</strong></div><div className="info-line"><span>Protein</span><strong>{Math.round(weight*2)}g / day</strong></div><div className="info-line"><span>Water</span><strong>{Math.round(weight*35/1000*10)/10}L / day</strong></div></div><button className="btn btn-primary" style={{marginTop:18}}>Save to profile</button></div></div></main>
}
